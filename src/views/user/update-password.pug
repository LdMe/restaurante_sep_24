extends layout

block content
  h1 Cambiar Contraseña


  form(action=`/user/${user.user_id}/update-password` method="POST")
    .form-group
      label(for="currentPassword") Contraseña Actual *
      input#currentPassword.form-control(
        type="password"
        name="currentPassword"
        required
      )

    .form-group
      label(for="newPassword") Nueva Contraseña *
      input#newPassword.form-control(
        type="password"
        name="newPassword"
        required
      )

    .actions
      button.btn.btn-primary(type="submit") Actualizar Contraseña
      a.btn.btn-secondary(href=`/user/${user.user_id}`) Cancelar